export const DOCUMENT_TITLE_PREFIX = 'GL';

export const DATE_PICKER_OPTIONS = {
  displayMode: 'dialog',
};

export const CHALLENGES = {
  NEW_PASSWORD_REQUIRED: 'NEW_PASSWORD_REQUIRED',
};

export const PASSWORD_REGEX = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d]{8,}$/;
